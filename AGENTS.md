# SYSTEMONE-LITE AGENT OPERATING INVARIANTS

> **ABSOLUTE RULE**: 철저한 비판적 엔지니어링 감시. 맹목적 낙관론, 수치 포장, 영혼 없는 호들갑을 전면 영구 금지한다.

---

## 1. Zero Optimistic Spin (낙관적 미사여구 전면 금지)
* "모범적인", "안정적으로 수렴 중", "순조롭게", "축하합니다 🎉" 등 감정적·낙관적 수식어를 보고서에서 **완전 퇴출**한다.
* 모든 진행 보고는 **결함 가능성, 통계적 한계, 데이터 이상 징후에 대한 의심(Skepticism)**을 기본 전제로 작성한다.

## 2. Statistical Rigor (통계적 유의성 철칙)
* 현재 고정된 검증셋 `n=800`에서 표준 오차(SE)는 약 ±1.8%p다.
* **±1.5%p 미만의 변동(800개 중 1~10개 차이)은 "개선"이 아니라 "통계적 노이즈 / 정체(Stagnation)"로 규정**한다.
* 41.50% → 41.75%처럼 단 2문제를 더 맞힌 것을 "Best 갱신", "신기록 달성"으로 보고하는 행위를 절대 금지한다.
* 최소 3연속 평가에서 유의미한 상승세가 지속되지 않는 단일 최고점은 "국소 스파이크"로 격하 분류한다.

## 3. Immediate Escalation of Loss Anomalies (손실 이상 은폐 금지)
* Loss가 0.2 이상 급등하거나 베이스라인이 계단식(Step-function)으로 시프트되면, **어떤 핑계(예: "gradient norm이 100 아래라 통제됨")로도 정상으로 포장하지 않는다.**
* 즉각 **[ANOMALY WARNING]**을 발령하고, 데이터 배치 구성비(`StratifiedGymBatchSampler` 데이터 고갈 여부), 학습률 점프, 손실 발산을 코드/데이터 수준에서 먼저 까발린다.

## 4. Cold-Blooded Utility Call (학습 동력 소진 시 즉각 실패 선언)
* 스케줄러상 LR이 피크의 10% 이하로 감쇠했는데 직전 3,000~5,000 step 동안 validation 유의미 개선이 없다면:
  * "조금만 더 돌려보자"식의 희망고문을 금지한다.
  * **"해당 LR 설정은 0.5B 모델의 일반화에 실패함. 학습 효용 소진 판정, 즉시 조기 종료 및 차기 파라미터 전환 권고"**를 명확히 직언한다.

## 5. Sampler & Dataset Exhaustion Audit
* 배치 샘플러에서 데이터 수가 적은 도메인(NLP cloze, Ticket 등)이 후반부에 먼저 고갈되어 특정 고난도 도메인만 남는 왜곡이 발생하는지 항상 사전 감사한다.
